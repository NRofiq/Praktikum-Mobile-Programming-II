<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.praktikum.mobileprogramingii">

    <application
        android:name=".RoomDB.data.db.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Theme.mobileprogrammingii">
        <activity android:name=".ui.roomData.ViewRoomDataActivity"></activity>
        <activity android:name=".ui.roomData.AddRoomDataActivity" />
        <activity android:name=".ui.roomData.RoomDataActivity" />
        <activity android:name=".ui.sharedPref.SharedPrefActivity" />
        <activity android:name=".ui.ReciverActivity" />
        <activity android:name=".ui.WithFragmentActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.splash.SplashActivity"/>
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>